{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "BetterVanilla UI Aliases",
  "description": "Schema for ui-aliases.json configuration file",
  "type": "object",
  "required": ["version", "aliases"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Reference to the JSON schema"
    },
    "version": {
      "type": "string",
      "description": "Version of the aliases configuration format",
      "pattern": "^\\d+\\.\\d+$"
    },
    "defaultBundle": {
      "type": "string",
      "description": "Default AssetBundle name to use when not specified in alias"
    },
    "defaultNamespace": {
      "type": "string",
      "description": "Default namespace for control components"
    },
    "aliases": {
      "type": "object",
      "description": "Map of alias names to their definitions",
      "additionalProperties": {
        "$ref": "#/definitions/aliasDefinition"
      }
    }
  },
  "definitions": {
    "aliasDefinition": {
      "type": "object",
      "description": "Definition of a UI element alias",
      "required": ["prefab", "component"],
      "properties": {
        "prefab": {
          "type": "string",
          "description": "Path to the prefab in the AssetBundle"
        },
        "component": {
          "type": "string",
          "description": "Full type name of the control component"
        },
        "bundle": {
          "type": "string",
          "description": "Override the default bundle for this alias"
        },
        "defaultProperties": {
          "type": "object",
          "description": "Default property values for this alias",
          "additionalProperties": true
        }
      }
    }
  }
}
